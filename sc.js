var shapeCalculator = {
    calculateArea(length, width) {
        console.log("Current are is ", length * width, "m2")
    },
    calculatePerimeter(length, width) {
        console.log("Current area is ", 2* length + 2 * width, "m")
    }
}